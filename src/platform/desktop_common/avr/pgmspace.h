// avr/pgmspace.h — desktop stub. AVR's Harvard-architecture flash-strings
// API. On flat-memory targets, all the macros collapse to plain memory
// access (already provided by Arduino.h's PROGMEM family). This file exists
// so MCL source that #include's <avr/pgmspace.h> compiles.
#pragma once

#include "Arduino.h"
