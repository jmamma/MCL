#include "DSP.h"

float DSP::saturate(float sample, float max) {
    return sample;
/*
    if (sample > max) {
        return max;
    }
    if (sample < -max) {
        return -max;
    }
    return sample;
 */
}
DSP dsp;
